/**
 * Props for the BottomActionBar component.
 */
export interface BottomActionBarProps {
  /**
   * Whether the component should render in mobile mode.
   * @default false
   */
  isMobile?: boolean;
}
