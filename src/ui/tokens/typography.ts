/**
 * Font weight names — match $typo-font-weight-* in typography-variables.scss.
 * Values resolve at runtime via var(--font-weight-{name}).
 */
export type FontWeight = 'light' | 'regular' | 'medium' | 'semibold' | 'bold';
